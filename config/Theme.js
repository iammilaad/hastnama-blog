const colors = {
  primary: "#ffb142", // Color for buttons or links
  bg: "#222", // Background color
  white: "#fff",
  bodyColor: "#b9c3ca",
  grey: {
    dark: "#b9c3ca",
    default: "#b9c3ca",
    light: "#b9c3ca",
    ultraLight: "#b9c3ca"
  }
};

const transitions = {
  normal: "0.5s"
};

const fontSize = {
  small: "0.9rem"
};

const theme = {
  colors,
  transitions,
  fontSize
};

export default theme;
